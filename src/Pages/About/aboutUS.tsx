import React from 'react';
import {AboutUsProps} from "./aboutUsProps";


const About = (AboutUsProps: AboutUsProps) => {
    // let {project} = useLoaderData();
    return (
        <h1>
            Hi, <br/>This is Ahmad
        </h1>
    );
};

export default About;
